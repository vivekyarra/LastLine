# IBM Bob development evidence — 2026-09-03

LastLine used IBM Bob Shell 2.0.2 during development under the free IBM Bob trial. The trial was provisioned for India with the optional company field blank and optional marketing consent left unselected.

## Task given to Bob

> Audit `agent/app/policy.py` and `agent/tests/test_policy.py` for one concrete actor-release safety regression gap. Implement the smallest useful test or fix, run only the relevant backend tests, and report exact files and result. Stay strictly within LastLine actor-release scope and add no dependency.

## Material contribution

Bob identified an untested fail-closed boundary: a high-confidence evidence candidate whose `recording_id` does not exist in the submitted recording inventory must never clear an owed line.

Bob added `test_candidate_with_phantom_recording_id_does_not_clear` to `agent/tests/test_policy.py`. The test constructs an otherwise-perfect evidence candidate tied to `PHANTOM-999` and proves that the decision remains `hold` with `S12-L7` unresolved.

No production behavior or dependency was changed because the policy implementation already rejected the phantom path. Bob strengthened regression coverage around that safety invariant.

## Verification

Bob's final focused command ran from `agent/`:

```powershell
& ".venv\Scripts\python.exe" -m pytest tests/test_policy.py -v
```

Result:

```text
6 passed in 0.10s
```

Codex independently reran the same focused suite after the Bob session:

```text
6 passed in 0.10s
```

## Exact changed artifact

- `agent/tests/test_policy.py`

This record intentionally claims only what was observed: Bob performed a scoped repository audit, authored one safety regression test, and verified the focused policy suite. IBM Bob is a development partner for the IBM track; it is not presented as part of LastLine's runtime architecture.
